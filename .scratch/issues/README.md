# Implementation tickets — orthodox-prayer-toolkit

| Location | Meaning |
|----------|---------|
| `issues/*.md` | Open work (`ready-for-agent`, …) |
| `issues/done/*.md` | Finished tickets — do **not** re-implement |

**Open frontier:** none. Content outline `31`–`34` done. Spec: `../editor-toc-spec.md`.

Completed earlier: Layout export `27`–`30` (see `issues/done/`). Spec: `../layout-export-spec.md`.

Specs: `../spec.md`, `../html-export-spec.md`, `../layout-export-spec.md`, `../editor-toc-spec.md`.

See `docs/agents/issue-tracker.md` for status rules and frontier selection.
