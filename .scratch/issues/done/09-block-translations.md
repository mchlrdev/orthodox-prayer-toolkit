# 09 — Block-Übersetzungen editieren

**What to build:** Für die aktive Variante Text-Payloads (`text` / `lines` je nach kind) an Blöcken setzen; fehlende Übersetzung = kein Eintrag; speichern.

**Blocked by:** 07 — Varianten-Registry editieren; 08 — Structure-Blöcke: Gerüst editieren

**Status:** done

- [x] Aktive Variante steuert, welche Übersetzung bearbeitet wird
- [x] `text` bzw. `lines` werden korrekt persistiert
- [x] Leeren entfernt den Übersetzungs-Eintrag (keine leeren Keys)

**Completed:** Implemented in the living toolkit; archived so agents do not re-pick this ticket.
