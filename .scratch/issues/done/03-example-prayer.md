# 03 — Beispielgebet in Dev-Library

**What to build:** Mindestens ein Beispiel-JSON in der Toolkit-Dev-Library (eine Sprache vollständig, zweite Variante teilweise), das der Validator akzeptiert.

**Blocked by:** 02 — Prayer-Schema + Validate in Core

**Status:** done

- [x] Beispiel folgt Modell A (shared structure, variants registry, per-block translations)
- [x] `validate` akzeptiert die Datei
- [x] Kein Prod-Korpus — nur Dev-Beispiel

**Completed:** Implemented in the living toolkit; archived so agents do not re-pick this ticket.
