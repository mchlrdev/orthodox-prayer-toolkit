# 18 — Library-Style-Override

**What to build:** Kind-Styles als Library-Override speichern; Resolve-Regel Library > App-Defaults; neu entdeckte kinds bekommen Default und sind überschreibbar.

**Blocked by:** 17 — Style-Panel + App-Defaults persistent

**Status:** done

- [x] Override liegt bei der Library (nicht in Gebets-JSON)
- [x] Resolve: Library überschreibt App-Defaults
- [x] Andere Library ohne Override fällt auf App-Defaults zurück

**Completed:** Implemented in the living toolkit; archived so agents do not re-pick this ticket.
